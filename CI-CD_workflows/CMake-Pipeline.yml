trigger:
  branches:
    include:
    - main

pool:
  name: Test-Repo-Agent-Pool

stages:
- stage: Build
  jobs:
  - job: Build
    steps:
    - task: CMakeToolInstaller@1
      inputs:
        cmakeVersion: '3.21.x'  # Specify the version of CMake you want to install
        addToPath: true 
