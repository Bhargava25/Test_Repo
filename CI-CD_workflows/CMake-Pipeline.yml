trigger:
  branches:
    include:
    - main

pool:
  name: Test-Repo-Agent-Pool

stages:
- stage: Build
  jobs:
  - job: Build
    steps:
    - script: |
        cmake .
      displayName: 'Configure Project with CMake'
